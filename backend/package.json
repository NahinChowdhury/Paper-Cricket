{
	"name": "paper-cricket-backend",
	"version": "1.0.0",
	"description": "Backend server for multiplayer spinning pie game",
	"main": "dist/server.js",
	"scripts": {
		"format": "prettier --write \"**/*.ts\" \"**/*.json\"",
		"dev": "npm run format && nodemon --config nodemon.json src/server.ts",
		"dev:debug": "npm run format && nodemon --config nodemon.json src/server.ts",
		"debug": "npm run format && nodemon --config nodemon.json src/server.ts",
		"start": "npm run format && nodemon --config nodemon.json dist/server.js",
		"start:debug": "npm run format && nodemon --config nodemon.json dist/server.js",
		"build": "npm run format && tsc",
		"prod": "npm run build && npm start"
	},
	"dependencies": {
		"socket.io": "^4.7.2",
		"express": "^4.18.0",
		"cors": "^2.8.5",
		"uuid": "^9.0.0",
		"dotenv": "^16.0.0"
	},
	"devDependencies": {
		"typescript": "^5.0.0",
		"@types/express": "^4.17.0",
		"@types/cors": "^2.8.0",
		"@types/uuid": "^9.0.0",
		"@types/node": "^20.0.0",
		"ts-node-dev": "^2.0.0",
		"nodemon": "^2.0.0",
		"prettier": "^3.0.0"
	}
}
